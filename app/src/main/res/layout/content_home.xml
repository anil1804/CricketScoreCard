<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:app="http://schemas.android.com/apk/res-auto"
	xmlns:tools="http://schemas.android.com/tools"
	android:layout_width="match_parent"
	android:layout_height="match_parent"
	app:layout_behavior="@string/appbar_scrolling_view_behavior"
	tools:context=".activity.HomeActivity"
	tools:showIn="@layout/app_bar_home">

	<RelativeLayout
		android:layout_width="match_parent"
		android:layout_height="match_parent">

		<Button
			android:id="@+id/btnLimitedOvers"
			android:text="@string/limited_overs_cricket"
			android:layout_above="@id/btnManagePlayer"
			android:drawableStart="@drawable/ic_cricket_limited_overs"
			android:layout_centerHorizontal="true"
			style="@style/ButtonStyle"/>

		<Button
			android:id="@+id/btnManagePlayer"
			android:text="@string/managePlayer"
			android:layout_centerInParent="true"
			style="@style/ButtonStyle"/>

	</RelativeLayout>

</android.support.constraint.ConstraintLayout>